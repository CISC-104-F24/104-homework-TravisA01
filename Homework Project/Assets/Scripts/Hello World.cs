using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloWorld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hello, world!");
        Debug.LogWarning("I am a warning");
        Debug.LogError("This is an error");
        // Author: Travis Anderson
        /*
         *Is there usually this much trial and error in programming?
         *How much of what we learn is just like this?
         *Would a more complex game have more complex code?
         */

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
